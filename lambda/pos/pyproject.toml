[project]
name = "pos"
version = "0.1.0"
description = "POS端末専用API（端末認証・従業員認証・販売処理）"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "boto3>=1.35.0",
    "fastapi>=0.115.0",
    "mangum>=0.19.0",
    "pydantic>=2.9.0",
    "pynacl>=1.5.0",  # Ed25519署名検証用
]
